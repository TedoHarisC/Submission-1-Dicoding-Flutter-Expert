import 'package:flutter/material.dart';

class DetailTVPage extends StatelessWidget {
  static const routeName = '/page_detail_tv';
  final int id;

  const DetailTVPage({super.key, required this.id});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
